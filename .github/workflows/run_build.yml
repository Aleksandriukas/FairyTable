name: Run build
run-name: ${{ github.actor }} is building React Native "Fairy Table" application.
on: [push]
jobs:
  Build app:
    runs-on: ubuntu-latest
    steps:
      - run: echo "filename=test_$(date +'%Y-%m-%d_%H-%M').txt" >> $GITHUB_ENV
      - run: echo "The job was triggered by a ${{ github.event_name }} event."
      - run: echo $filename
      - run: echo "This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out branch code
        uses: actions/checkout@v4
      - run: echo "The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "The workflow is now ready to test your code on the runner."
      - name: docker run
        COPY: . .
      - run: docker build . -t react-app
      - run: docker run -p 6080:6080 
